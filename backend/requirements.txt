# FastAPI and ASGI server
fastapi==0.115.5
uvicorn[standard]==0.32.0

# Data validation (latest with pre-built wheels for Python 3.14)
pydantic>=2.10
pydantic-settings==2.6.1

# Database - PostgreSQL (using psycopg3 for better Windows compatibility)
sqlalchemy==2.0.36
psycopg[binary]==3.3.2
asyncpg==0.30.0

# Database - MongoDB
pymongo==4.10.1
motor==3.7.1  # Async MongoDB driver

# Redis for caching
redis>=5.0.0
hiredis>=3.0.0

# Async SQLite (for testing)
aiosqlite>=0.20.0

# Environment and config
python-dotenv==1.0.1

# Authentication
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# HTTP client
httpx==0.28.1
requests>=2.32.0  # For verification scripts

# AI/LLM
openai>=2.14.0  # OpenAI API client

# Utilities
tenacity==9.0.0
structlog==24.4.0
psutil>=7.2.0  # System and process utilities

# PHASE B5: Observability
prometheus-client==0.20.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0

# Embedding model (AI Engines)
sentence-transformers>=2.2.0
torch>=2.0.0

# PHASE THREE: Learning Analytics
scikit-learn>=1.3.0  # For local classification in mastery modeling

# PHASE SIX: Mobile & Low-Connectivity Resilience
cryptography>=41.0.0  # For answer buffer encryption (AES-256)
