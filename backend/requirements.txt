# FastAPI and ASGI server
fastapi==0.115.5
uvicorn[standard]==0.32.0

# Data validation (latest with pre-built wheels for Python 3.14)
pydantic>=2.10
pydantic-settings==2.6.1

# Database - PostgreSQL (using psycopg3 for better Windows compatibility)
sqlalchemy==2.0.36
psycopg[binary]==3.3.2
asyncpg==0.30.0

# Database - MongoDB
pymongo==4.10.1

# Redis for caching
redis>=5.0.0
hiredis>=3.0.0

# Async SQLite (for testing)
aiosqlite>=0.20.0

# Environment and config
python-dotenv==1.0.1

# Authentication
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# HTTP client
httpx==0.28.1

# Utilities
tenacity==9.0.0
structlog==24.4.0

# PHASE B5: Observability
prometheus-client==0.20.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0

# Embedding model (AI Engines)
sentence-transformers>=2.2.0
torch>=2.0.0

